# Haydar Pack V54 — Documents Pro

## الجديد
- إضافة `assets/js/12-documents-pro.js`.
- نظام أرقام مستندات ثابتة:
  - `HP-YYYY-QT-0001`
  - `HP-YYYY-INV-0001`
  - `HP-YYYY-CS-0001`
  - `HP-YYYY-FS-0001`
- حفظ Snapshot كامل للمستند داخل `DB.documents` حتى يمكن إعادة فتح نفس المستند بنفس الرقم.
- حالات للمستندات: `Draft / Sent / Paid / Cancelled`.
- مركز مستندات داخل صفحة التقارير يعرض آخر المستندات وإعادة فتحها وطباعتها.
- تصدير سجل المستندات CSV و JSON.
- تطوير الفاتورة/عرض السعر/كشف العميل/كشف المصنع بدون حذف النسخ القديمة.

## الأمان
- لا يوجد تعديل Apps Script.
- المستندات الجديدة تتحفظ داخل الداتا نفسها، وبالتالي تدخل في المزامنة والـ Backup الحالي.
- تم الحفاظ على V49.1 / V50 / V51 / V52 / V53.3.
